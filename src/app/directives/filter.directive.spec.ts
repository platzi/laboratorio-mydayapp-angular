import { FilterDirective } from './filter.directive';

describe('FilterDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterDirective();
    expect(directive).toBeTruthy();
  });
});
